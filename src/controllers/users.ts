import { Router } from "express";

const usersRouter = Router();

usersRouter.get("/", (_request, _response) => {
})

usersRouter.post("/", (_request, _response) => {
})

usersRouter.get("/:id", (_request, _response) => {
})

usersRouter.patch("/:id", (_request, _response) => {
})

export { usersRouter };